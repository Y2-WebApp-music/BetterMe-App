import { View, Text } from 'react-native'
import React from 'react'
import { useAuth } from '../../../context/authContext';

const Menu = () => {
  return (
    <View>
      <Text>Menu</Text>
    </View>
  )
}

export default Menu