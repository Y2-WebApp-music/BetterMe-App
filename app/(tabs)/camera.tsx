import { View, Text, SafeAreaView } from 'react-native'
import React from 'react'

const Camera = () => {
  return (
    <SafeAreaView>
      <Text>Camera</Text>
    </SafeAreaView>
  )
}

export default Camera